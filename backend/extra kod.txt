read.php:
//Med gender hämta en eller flera specifika users
        if(isset($_GET["genders"])) {
            $gender = explode(",", $_GET["genders"]);
            $usersByGender = [];

            //Loop för att ta ut user baserat på ett gender
            foreach($data as $user) {
                if(in_array($user["gender"], $gender)) {
                    $usersByGender[] = $user;
                }
            }
            //Skriver ut users baserat på gender
            sendJson($usersByGender);
        }